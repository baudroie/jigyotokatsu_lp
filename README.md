# baudroie NEXT LEADER 採用LP

## 最新の修正：SPキャリアとLINEリンク

- SPキャリアの「＋」4点と「キャリアの詳細を見る」ボタンを撤去しました。カードの表示幅・自然比率を保ち、画像を中央揃えにしています。
- HEROと最終CTAのLINEボタン2か所に指定URLを設定しました。JavaScript無効時もHTMLのリンク先が有効です。
- 今回の検証記録は `comparison/20260831-213233-career-line/`。下記の新素材対応時の比較結果・未達事項は当時の記録として保持しています。公開・デプロイは行っていません。

## 新素材ZIPの14枚へ差し替え

2026-08-31の新素材対応は `comparison/new-assets-20260831-202530/visual-qa.md` を参照してください。以下の旧修正履歴はそのまま保存していますが、現在のカード実装は新素材版です。

- 訴求3枚とキャリア4枚を、それぞれPC/SP専用PNGへ置換。`picture/source` は767px以下でSP、768px以上でPCを選択します。
- 各画像は完成カードとして全体表示。旧合成画像の部分表示・カードの負のmargin・固定高さ・二重背景を撤去しました。
- 新PNGは `assets/cards/new-20260831/` と `assets/career/new-20260831/`。ZIP原本、旧PNG、以前の比較成果物は保持しています。
- 寸法・透過領域・元ZIPメンバー・配置先・SHA-256は今回の `asset-map.json`、現在の使用パス一覧はルートの `asset-map.json` に記録しています。
- PC 4列/SP縦4枚、タブレット768〜1023pxのみ既存の2列を維持。1101〜1439pxは既存の中間幅と1440pxの値を補間しています。
- ナビとCTAは既存の動作を維持。キャリアの「＋」・先輩ページドットには元々開閉/切替機能はありません。今回も新機能は追加せず、装飾画像として扱っています。
- **素材差し替えは完了、完成デザインへの全体寸法一致は未達**です。新SP素材は縦長のため、全内容表示を守ると390px幅でページが約2762px長くなります。PCもカード比率が異なり約170px短くなります。詳細な推奨素材比率は今回の検証記録を参照してください。
- 公開・デプロイは実施していません。

## 起動方法

`index.html` を直接ブラウザで開くか、このフォルダで静的HTTPサーバーを起動してください。

```sh
python3 -m http.server 4173
```

## 使用アセット

元フォルダの画像は変更せず、実装用に `assets/` へコピーして英数字名へ整理しています。対応関係は `asset-map.json` を参照してください。Hero、3つの訴求カード、黒背景図解、フィールド、キャリア、先輩、選考フロー、最終CTAの提供画像を使用しています。

## LINE URL

`script.js` 先頭の `LINE_URL` に指定URLを設定済みです。将来変更する際は、JavaScript無効時のフォールバックとして `index.html` の `.line-link` 2か所の `href` も同じURLへ変更してください。

現在のURL: `https://s.lmes.jp/landing-qr/1657893371-rRVdmjzQ?uLand=Pfwucb`

## ブレークポイント

- PC: 1024px以上
- Tablet: 768〜1023px
- SP: 767px以下

## 主要セクション

Hero / 3つの訴求カード / なぜ最前線で働けるのか / あなたに任されるフィールド（PCのみ） / NEXT LEADERのキャリアパス / 先輩事例 / 選考フロー / 最終CTA / Footer

## 未設定リンク・今後の差し替え

- プライバシーポリシー、利用規約: 実URL未提供のため `#` です。

## 2026-08-31 の見切れ・レイアウト修正

- `visual-qa.md`: 変更前と7ループの修正履歴、セクション境界、残存NG。
- `layout-metrics.json`: 1440/390pxのReference/Currentのtop・bottom・height。
- `image-layout-audit.json`: 全imgの元サイズ・描画サイズ・親サイズ・overflow・祖先クリッピング・比率検査。
- `comparison/20260831-loop7/`: 最新PC/SP全体スクリーンショット、50%overlay、side-by-side、diff、機能テスト。
- `qa_layout.py` と `visual_compare.py`: 保存した撮影結果から比較画像・境界レポートを再生成。元画像への書き込みは行わない。

キャリア・人物・タイトル・CTAは自然比率の全画像表示です。透明なcanvasの余白だけをレイアウト上相殺し、overflowやマスクで隠していません。Careerの非操作画像は透明部分がCTAのクリックを奪わないようpointer-eventsを無効にしています。

### 未完了の制約

- 3つの訴求カードは見出しと3枚が一体になったPNGしかありません。既存の部分表示を完全に撤去しつつ縦3段にするには、個別PNGの追加または素材抽出の許可が必要です。現在はカード本体・文字・イラスト・下枠を含む範囲を表示し、CSSの二重カードは撤去済みですが、PNG全体表示の条件は未達です。
- 提供キャリアPNG内の完成カードはSP正解より縦長です。説明文・人物・年収を全表示し、読める幅を維持するとSPキャリアはお手本より約357px長くなります。画像を歪めたり本文を切ったりして差を隠していません。
- このため現在の総合QAはblockedであり、「お手本と完全一致して完了」とは判定していません。
